{
  "name": "Documents API",
  "description": "Document Management System API",
  "repository": "https://github.com/thuo/documents-api",
  "env": {
    "SECRET_TOKEN": {
      "description": "A secret key for verifying signed tokens.",
      "generator": "secret"
    },
    "MONGODB_URI": {
      "description": "The URI to the mongodb database to use.",
      "required": true
    },
    "NODE_ENV": {
      "description": "The kind of environment to run the app in.",
      "value": "production"
    },
    "ADMIN_EMAIL": {
      "description": "The email for the default admin account.",
      "value": "admin@example.com"
    },
    "ADMIN_PASSWORD": {
      "description": "The password for the default admin account."
    }
  }
}
